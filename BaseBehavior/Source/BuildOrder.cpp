#include "BuildOrder.h"

void ZergBuildOrder::checkMilitaryRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition,max_distance).end(); i++){

	}
}

void ZergBuildOrder::checkResourceRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}

void ZergBuildOrder::checkSpecialRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}





void TerranBuildOrder::checkMilitaryRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}

void TerranBuildOrder::checkResourceRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}

void TerranBuildOrder::checkSpecialRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}





void ProtossBuildOrder::checkMilitaryRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {
		
	}
}

void ProtossBuildOrder::checkResourceRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}

void ProtossBuildOrder::checkSpecialRequisitions(set<UnitTypes> *requisitions, Position basePosition) {
	for (std::set<Unit *>::const_iterator i = Broodwar->getUnitsInRadius(basePosition, max_distance).begin(); i != Broodwar->getUnitsInRadius(basePosition, max_distance).end(); i++) {

	}
}
